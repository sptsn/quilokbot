.row
  .col-md-6
    .page-header
      h1
        = t('residents.edit')

    = render partial: 'residents/shared/form', locals: { resident: resource_resident, url: resident_path(resource_resident), submit_text: t('update') }
